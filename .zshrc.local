. `brew --prefix`/etc/profile.d/z.sh
alias -s rb=$EDITOR
alias -s feature=$EDITOR
alias -s erb=$EDITOR
alias -s haml=$EDITOR
alias -s js=$EDITOR
alias -s scss=$EDITOR
alias -s coffee=$EDITOR
alias -s cs=$EDITOR
alias -s rake=$EDITOR
export SPRING_LOG=/tmp/spring.log

export PATH="$HOME/.bin:$PATH"
eval "$(rbenv init - zsh --no-rehash)"
